<Window x:Class="CyberSecurityChatbot.MainWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Title="Cybersecurity Awareness Bot" Height="650" Width="900" WindowStartupLocation="CenterScreen" Background="#121212">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <!-- HEADER -->
        <Border Grid.Row="0" Background="#1F1F1F" CornerRadius="15" Padding="15" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="Cybersecurity Awareness Chatbot" FontSize="30" FontWeight="Bold" Foreground="#4FC3F7" HorizontalAlignment="Center"/>
                <TextBlock Text="Stay Safe Online" FontSize="16" Foreground="White" HorizontalAlignment="Center" Margin="0,5,0,10"/>
                <!-- ASCII ART -->
                <Border Background="#0D0D0D" CornerRadius="10" Padding="10">
                    <TextBlock FontFamily="Consolas" Foreground="LightGreen" FontSize="12" TextAlignment="Center" Text=" _______ __ __ _______ _______ _______ | || | | || || || | | || |_| || ___|| _____|| _____| | || || |___ | |_____ | |_____ | _|| || ___||_____ ||_____ | | |_ | _ || |___ _____| | _____| | |_______||__| |__||_______||_______||_______| CYBER SECURITY BOT "/>
                </Border>
            </StackPanel>
        </Border>
        <!-- CHAT AREA -->
        <Border Grid.Row="1" Background="#1E1E1E" CornerRadius="15" Padding="15">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="ChatDisplay" Foreground="White" FontSize="16" TextWrapping="Wrap"/>
            </ScrollViewer>
        </Border>
        <!-- INPUT -->
        <Grid Grid.Row="2" Margin="0,15,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="120"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="UserInput" Grid.Column="0" Height="45" FontSize="16" Padding="10" Background="#2B2B2B" Foreground="White" BorderBrush="#4FC3F7" KeyDown="UserInput_KeyDown"/>
            <Button Grid.Column="1" Content="Send" Height="45" Margin="10,0" FontSize="16" Background="#4FC3F7" Foreground="Black" FontWeight="Bold" Click="SendButton_Click"/>
            <Button Grid.Column="2" Content="Play Voice" Height="45" FontSize="16" Background="#81C784" Foreground="Black" FontWeight="Bold" Click="VoiceButton_Click"/>
        </Grid>
    </Grid>
</Window>